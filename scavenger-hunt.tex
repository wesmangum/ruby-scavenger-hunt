\documentclass{beamer}
\usepackage{amsmath}
\title{Ruby Lang Scavenger Hunt}

\begin{document}
    \frame{\titlepage}
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{Goals}
        \begin{center}
            Accelerate your understanding of ruby through practice and documentation usage.
            \newline
            \newline
            You are allowed to use any previous code you've written and rdoc.
        \end{center}
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{The Hunt}
        \textbf{Task}: Find the official ruby implementation of \textbf{Vector}. Create a class that inherits \textbf{Vector} and implements a $distance$ instance method that calculates the total distance (path length) between 2 points.
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{}
        \textbf{Task}: With your class from the previous task, implement a $pathlength$ \textbf{class method} that calculates the total path length for $n$ points.
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{The Hunt}
        \textbf{Task}: Determine whether or not if an expression \textbf{Vector} + \textbf{Integer} is valid. Write a test that demonstrates the expected behavior of adding an integer to a vector.
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{The Hunt}
        \textbf{Task}: Determine whether or not if an expression $\mathbf{Vector} << Integer$ is valid. Write a test that demonstrates the expected behavior of inserting an integer to a vector. Implement the $<<$ operator to allow for the insertion of an integer into an existing vector.
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{The Hunt}
        \textbf{Task}: 
        \begin{enumerate}
            \item Write a test that demonstrates the intended behavior of a \textbf{class method} that calculates the angle between two $2-Vectors$ in radians.
            \item Write a test that demonstrates the intended behavior of a \textbf{instance method} that calculates the angle between two $2-Vectors$ in radians.
            \item Implement both methods on your \textbf{Vector} subclass.
        \end{enumerate}
    }
    \frame{
        \frametitle{Scavenger Hunt}
        \framesubtitle{The Hunt}
        \textbf{Task}
        \begin{enumerate}
            \item Write a test that demonstrates the expected behavior of an instance method $factors$ in the \textbf{Integer} class that returns the factors of a number.
            \item Implement this method by opening the \textbf{Integer} class.
        \end{enumerate}
    }
\end{document}
